import java.util.*;
//44%
class Solution {
    public int solution(int[] A) {
        Arrays.sort(A);
        return A[A.length - 3] * A[A.length - 2] * A[A.length - 1];
    }//...and if there are two negatives, A[0]*A[1]*A[length-1] is needed to be checked!
}
